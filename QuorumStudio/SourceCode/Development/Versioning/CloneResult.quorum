package Libraries.Development.Versioning

class CloneResult
    boolean success = true
    text message = ""
    action GetMessage returns text
        return message
    end

    action SetMessage(text message)
        me:message = message
    end

    
    action IsSuccessful returns boolean
        return success
    end

    action SetIsSuccessful(boolean success)
        me:success = success
    end
end