package Libraries.Security

use Libraries.System.File

class Cryptography
    system action Decrypt(text key, File inputFile) returns text
    system action Encrypt(text key, text contents, File outputFile)
end