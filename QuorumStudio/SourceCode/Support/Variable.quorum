package Libraries.Language.Debug

class Variable 
    system action IsField returns boolean
    system action IsWatchExpression returns boolean
    system action IsPublic returns boolean
    system action IsPrivate returns boolean
    system action GetName returns text
    system action GetTypeName returns text
    system action GetValue returns text
    system action IsPrimitive returns boolean
    system action IsParent returns boolean
    system action GetReference returns Object
end