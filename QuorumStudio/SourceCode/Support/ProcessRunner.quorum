package Libraries.Concurrency
use Libraries.Containers.Array
use Libraries.System.File

class ProcessRunner 
    system action Run(text name, Array<text> flags)
    system action SetDirectory(File folder)
end