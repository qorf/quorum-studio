package Libraries.Development.Environment.Studio.Behaviors

use Libraries.Interface.Events.BehaviorEvent
use Libraries.Interface.Controls.Scenes.Items.EditorCursor
use Libraries.Interface.Controls.Scenes.SceneEditor_LIBRARY

class CursorSelectItemsBehavior is QuorumStudioBehavior

    action Run(BehaviorEvent event)
        if event:GetItem() is SceneEditor_LIBRARY
            SceneEditor_LIBRARY controller = cast(SceneEditor_LIBRARY, event:GetItem())
            EditorCursor cursor = controller:GetCursor()
            controller:SetFilteredItems(cursor:GetItemsAtCursor(), true)
        end
    end

end